CREATE LISTING MODEL

CREATE BOOKING MODEL

CREATE CART MODEL

CREATE LISTING ENDPOINTS (all vans, van by id, ...)

ADD ADMIN ROLE FEATURE

CREATE BOOKING PROCESS

CREATE CRUD API FOR LISTINGS, BOOKING

SHOW USERS IF A VAN IS AVAILABLE TO RENT OR NOT

ALLOW VAN RENTERS TO CREATE A RENTER ACCOUNT